    def findCenter(self, edges: List[List[int]]) -> int:
        a=edges[0][0]
        b=edges[0][1]
        if edges[1][0]==a or edges[1][1]==a:
            return a
        else:
            if edges[1][0]==b or edges[1][1]==b:
                return b   
